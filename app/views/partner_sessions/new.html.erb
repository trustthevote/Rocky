<h1>Voter Registration Partner Portal Log In</h1>

<p>
  Welcome to the Partner Portal of the <%= Settings.sponsor.name %> online voter registration tool.
  Please log in with your username and password below.
  If you do not have a partner account yet, you can
  <%= link_to "register", new_partner_path %> to get started.
</p>

<div class="login">
  <%= form_for @partner_session, :url => partner_session_path do |form| %>
    <fieldset>
      <legend>Log In</legend>
      <ul class="stacked">
        <li class="width_a">
          <h3><%= form.label :username, "Login" %></h3>
          <%= field_div form, :username %>
          <span class="error"><%= form.object.errors[:base].any? ? form.object.errors[:base] : form.object.errors[:username] %></span>
        </li>
      </ul>
      <ul class="stacked">
        <li class="width_a">
          <h3><%= form.label :password %></h3>
          <%= field_div form, :password, :kind => "password" %>
          <span class="error"><%= form.object.errors[:password] %></span>
        </li>
      </ul>
      <p><%= link_to "Forgot your password?", new_password_reset_path %></p>
    </fieldset>
    <%= partner_rollover_button("login", "Log in") %>
  <% end %>
</div>
