<h1>New registrant</h1>

<% form_for(@registrant) do |form| %>
  <%= form.error_messages %>
  <%= render :partial => 'form', :object => form %>
  <div class="row"><%= form.submit 'Submit' %></div>
<% end %>

<%= link_to 'Home', '/' %>